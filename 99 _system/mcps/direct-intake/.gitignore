# Venv + build artifacts (regenerated on every install)
.venv-direct-intake/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Local intake-log (runtime audit trail, not source)
# Logged to 99 _system/intake-log/ at the vault root.
# These files are regenerated on every test run.
