# Raw

> Captured source trails pulled in for cross-checking. NOT original research — these are snapshots of the artifacts the Verifier is auditing.

## Convention

```
raw/<agent>/<YYYY-MM-DDTHHMMSSZ>-<artifact-id>.json
raw/<agent>/<YYYY-MM-DDTHHMMSSZ>-<artifact-id>.md
```

Each raw file is a snapshot of the artifact at the time of audit, with a hash for integrity.

## Pending

*Empty. First CROSS_CHECK will populate.*

---

**Discipline:** Raw captures are reproducible from the source vaults. They are excluded from BACKUP for that reason. They are useful for re-audits when an artifact has since changed.
